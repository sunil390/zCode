---
ansible-navigator:
  execution-environment:
    image: localhost/ansible-execution-env
    pull-policy: never
    volume-mounts:
      - src: /home/sunil390/znext/vars
        dest: /vars:Z
      - src: /home/sunil390/znext/secrets
        dest: /secrets:Z
  mode: stdout
